# Local backend settings
# Copy this file to `.env` before starting the FastAPI app directly on your machine.

APP_NAME=Pose Memory Game API

# Local PostgreSQL connection for direct backend runs.
# When using docker compose, the backend service overrides this with the `db` hostname.
DATABASE_URL=postgresql+psycopg://posegame:posegame@localhost:5432/posegame
